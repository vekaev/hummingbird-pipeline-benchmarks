# The work, and the configurations it was measured on

Generated by `analysis/reporting.mjs`. The ledger below is committed data; every
per-stage number in the configuration table is recomputed from `results/raw/`.

## Changes made

In the order they were made. A change is only credited with an effect when a
measurement exists for it; the two marked "not measured" are implemented and
instrumented but have no number yet, and the reason is given.

| # | Change | What it does | Effect |
|---:|---|---|---|
| 1 | Recovered a per-stage profile from container logs | The pipeline's timing decorators printed to stdout and nothing persisted them. The platform retains container stdout, so the profile was pulled back over its API from real production runs with no instrumentation change. | Ten stages, n=5, reconciling to the independently printed job total within 0.03 s |
| 2 | Fixed the timing decorator so repeated stages accumulate | The decorator overwrote a stage's time on each call, so any job invoking a stage more than once reported only the last invocation. Every multi-segment word-replacement job was affected. | Every timing table on this site flows through the fixed code path |
| 3 | Pinned the dependency graph and made the image buildable | 28 of 62 requirement lines carried no version, so every rebuild resolved a different environment and nothing had rebuilt cleanly in roughly sixteen months. | 221 packages pinned; first green build of the repository since May 2025 |
| 4 | Found and fixed a mixed-CUDA torch stack | A version pin does not identify a build variant. Pinning torch to a version installed a CUDA 12.4 wheel from the public index, while the build's own later step installed CUDA 12.1 builds of torchaudio and torchvision. torchaudio then refuses to import, and the pipeline imports it before serving its first request. | The container reached a running state; a full clip completed in 398 s |
| 5 | Made the sampler seedable per run | The audio-to-expression model seeded once at construction, so the first prediction in a warm process was reproducible and every later one was not. Two runs of one clip shared zero identical frames out of 1074. | Prerequisite for a paired A/B, and for the cache in the same section |
| 6 | Renderer batch size 4 to 16 | Roadmap item, premised on the GPU being under-fed at batch 4. Exposed as one environment variable so a single build serves both sides of the comparison. | +0.32% slower, paired across three clips, all three the same direction: rejected |
| 7 | fp16 autocast in the neural renderer | Roadmap item. Also exposed as one environment variable, default off. | +0.51% with mixed per-clip signs, and 39.37 dB PSNR against the fp32 output: rejected |
| 8 | Asynchronous host-to-device transfers in the renderer | Roadmap item, partially implemented: non_blocking on six host-to-device copies. Pinned memory already existed; CUDA streams were not built. | **not measured** — Always on, so it is inside every arm including the baseline and cannot be isolated by this design |
| 9 | cuDNN autotuning scoped to the render loop | Not a roadmap item. The animator's seeding helper disables cuDNN autotuning process-wide as a side effect, and the renderer inherits it: hundreds of convolutions at one fixed input shape, run without autotuned kernels. | **not measured** — Arm was still running when this site was built |
| 10 | Paste-back composites only the mask bounding box | Where the warped mask is zero the composite writes the source back over itself. Restricting it to the mask's bounding box is bit-exact by construction rather than an approximation. | Bit-identical across 44 cases and 400 random transforms; 1.55x to 2.90x on the function, which is about 14% of the timed stage |
| 11 | Output sanity checks on every generated video | A timing harness cannot see a black frame, a frozen output, or a face whose mouth never moves. An arm that is faster because it produced garbage would otherwise read as a win. | 16 of 16 open-dataset outputs pass, confirmed by automated checks and by frame inspection |
| 12 | Made result writes concurrency-safe | The quality harness loaded its whole result file at start, held it for minutes, then wrote it back whole. Two concurrent scorings destroyed one another's results. | A completed three-clip result set was lost this way before the fix; writes now merge and rename atomically |

## Per-stage timing by configuration

The table the brief asked for: per-stage timing across several runs with hardware and
video parameters noted, one row per configuration.

Hardware is **measured** only for the A/B arms, where the runner writes the GPU model,
driver and torch build into every log header. For the production configurations the
container never prints its GPU, so hardware there is the deployment configuration and is
labelled as such — it is not an observation.

| Configuration | n | Crop | Landmarks | Parsing | Tracking | Animator | Geometry | Warp | Reshape | Render | Paste-back | Pipeline |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| Production lipsync | 5 | 30.7 | 7.8 | 39.7 | 146.6 | 6.7 | 19.7 | 62.0 | 13.0 | 129.6 | 73.1 | 528.8 |
| Production word replacement | 6 | 19.9 | 1.4 | 8.2 | 61.4 | 2.4 | 4.8 | 15.1 | 2.3 | 13.9 | 16.4 | 146.8 |
| HDTF self-driven | 12 | 19.2 | 5.4 | 28.4 | 112.9 | 4.2 | 14.0 | 51.6 | 9.0 | 88.6 | 17.7 | 351.0 |
| HDTF cross-driven | 4 | 19.6 | 5.9 | 28.6 | 113.2 | 4.4 | 13.9 | 47.3 | 9.1 | 87.9 | 18.6 | 348.4 |
| A/B arms | 18 | 18.0 | 5.8 | 36.6 | 141.6 | 4.6 | 20.9 | 47.7 | 7.9 | 90.8 | 12.5 | 386.4 |

All values are seconds, mean across the runs in that configuration. Rates and money
appear only in the cost tables, where the assumed rate is printed with them.

## Configuration key

Hardware and video parameters for each row of the table above. Hardware is an
observation only where marked measured; elsewhere it is the deployment configuration,
because the production container never prints the GPU it ran on.

| Configuration | Hardware | Video parameters | Deployment | Protocol |
|---|---|---|---|---|
| Production lipsync | A100, from deployment configuration (the container does not print its GPU) *(config, not measured)* | 1920x1080, 1074-1167 frames at 25 fps, 43.0-46.7 s | Deployed production build, serverless GPU, scale-to-zero | Driven predictions, one per log |
| Production word replacement | A100, from deployment configuration *(config, not measured)* | 1920x1080, one changed segment, 198 frames extracted then time-warped to 210 | Same deployed build, harvested from live traffic | Harvested, not driven; six replicas of one fixture |
| HDTF self-driven | A100, from deployment configuration *(config, not measured)* | 462-1026 px square, 751 frames at 25 fps, 30.0 s | Same deployed build, version pinned | A clip's own audio drives it, so ground truth exists |
| HDTF cross-driven | A100, from deployment configuration *(config, not measured)* | 462-1040 px square, 751 frames at 25 fps | Same deployed build, version pinned | One speaker's audio drives another's video; no ground truth by construction |
| A/B arms | **NVIDIA A100-SXM4-40GB, driver 580.105.08, torch 2.5.1+cu121, cuDNN 90100 - read from the log header, measured** | Three HDTF clips, 478-654 px square, 751 frames | Dedicated machine, image built from the pinned lock | One warm process per arm, sampler seeded, sequential, paired per clip |

## Measurement precision by configuration

Whether a configuration can resolve the effect being tested. The A/B arms were run on a
dedicated machine precisely because the production cohorts cannot: their clip-to-clip
spread is larger than the effects under test.

| Configuration | n | mean pipeline (s) | stdev (s) | CV | can resolve a 1% effect |
|---|---:|---:|---:|---:|---|
| Production lipsync | 5 | 528.77 | 18.33 | 0.035 | no |
| Production word replacement | 6 | 146.78 | 4.13 | 0.028 | no |
| HDTF self-driven | 12 | 350.99 | 25.21 | 0.072 | no |
| HDTF cross-driven | 4 | 348.41 | 12.73 | 0.037 | no |
| A/B arms | 3 | 389.21 | 4.01 | 0.010 | yes, paired |

