#!/usr/bin/env node
/**
 * Turns results/raw/ into the markdown tables every page reads.
 *
 * The page builders never compute a number; they lift tables from these files by heading.
 * So a heading here is an interface: renaming one breaks the build loudly, which is the
 * intent. Nothing on the site is typed by hand.
 *
 * Usage: node analysis/analyze.mjs
 */

import { readFileSync, writeFileSync } from 'node:fs';
import { dirname, join } from 'node:path';
import { fileURLToPath } from 'node:url';
import { mean, stdev, cv, percentile, pairedDelta, linfit, fmt, signed } from './stats.mjs';
import { RATES, DEFAULT_RATE, per1000, throughputPerGpuHour, usd } from './cost.mjs';

const root = join(dirname(fileURLToPath(import.meta.url)), '..');
const raw = (n) => JSON.parse(readFileSync(join(root, 'results/raw', `${n}.json`), 'utf8'));

const lipsync = raw('lipsync-prod');
const hdtfSelf = raw('hdtf-self');
const hdtfCross = raw('hdtf-cross');
const wr = raw('wordreplacement-prod');
const arms = raw('arms');
const lse = raw('lse');
const clips = raw('clips');

/** Stage order is the pipeline's execution order, which is also the article's argument. */
const STAGE_ORDER = ['crop_face', 'detect_landmarks', 'parse_face', 'track_face', 'run_animator',
  'create_driving_geo_and_mask', 'predict_liveportait', 'reshape_liveportrait', 'render_rgb', 'paste_back_video'];
const STAGE_LABEL = {
  crop_face: 'Face crop', detect_landmarks: 'Landmark detection', parse_face: 'Face parsing',
  track_face: '3D face tracking', run_animator: 'Audio to expression', create_driving_geo_and_mask: 'Driving geometry + mask',
  predict_liveportait: 'LivePortrait warp', reshape_liveportrait: 'Warp reshaping',
  render_rgb: 'Neural render + super-res', paste_back_video: 'Paste-back compositing',
};
/** Stages that run before the animator take no audio input: they are the cacheable prefix. */
const SOURCE_ONLY = new Set(['crop_face', 'detect_landmarks', 'parse_face', 'track_face']);
/** Job-level stages that touch no GPU. */
const CPU_ONLY = new Set(['download_and_preprocess', 'adjust_video_length_to_audio', 'modify_video_length',
  'split_segments', 'stitch_segments', 'attach_audio', 'upload_to_s3', 'upload_input_files_to_s3',
  'setup_job_dir', 'add_watermark_with_timing', 'generate_hashed_filename',
  'find_different_speech_segments_from_text_with_fragments']);

const stageMeans = (runs) => Object.fromEntries(STAGE_ORDER
  .filter((s) => runs.some((r) => s in r.stages))
  .map((s) => [s, mean(runs.filter((r) => s in r.stages).map((r) => r.stages[s]))]));

const pipelineOf = (r) => Object.values(r.stages).reduce((a, b) => a + b, 0);
const out = [];
const P = (s) => out.push(s);

// ---------------------------------------------------------------- stage profile
P('# Measured results\n');
P('Generated by `analysis/analyze.mjs` from `results/raw/`. Every table on the site is');
P('lifted from this file by heading; no number is typed by hand.\n');

function stageTable(runs, title, note) {
  const m = stageMeans(runs);
  const tot = Object.values(m).reduce((a, b) => a + b, 0);
  P(`## ${title}\n`);
  P(`${note}\n`);
  P('| Stage | mean (s) | median (s) | share | source-only |');
  P('|---|---:|---:|---:|---|');
  for (const s of Object.keys(m)) {
    const vals = runs.filter((r) => s in r.stages).map((r) => r.stages[s]);
    P(`| ${STAGE_LABEL[s] ?? s} | ${fmt(m[s])} | ${fmt(percentile(vals, 0.5))} | ${fmt(100 * m[s] / tot, 1)}% | ${SOURCE_ONLY.has(s) ? 'yes' : 'no'} |`);
  }
  P(`| **Pipeline total** | **${fmt(tot)}** | ${fmt(percentile(runs.map(pipelineOf), 0.5))} | 100% | |`);
  P('');
  return { m, tot };
}

const ls = stageTable(lipsync, 'Stage profile, production lipsync',
  `n=${lipsync.length} driven predictions, 1920x1080 source, ${Math.min(...lipsync.map((r) => r.frames))}-${Math.max(...lipsync.map((r) => r.frames))} frames at 25 fps. ` +
  `Stages sum to ${fmt(ls_total(lipsync))} s against an independently printed job total, unattributed difference below 0.05 s.`);
function ls_total(runs) { return mean(runs.map(pipelineOf)); }

const wrp = stageTable(wr, 'Stage profile, production word replacement',
  `n=${wr.length} harvested production predictions. This cohort is six replicas of one fixture — same source, same transcript, one changed segment — so it describes one workload measured six times, not six workloads.`);

const hs = stageTable(hdtfSelf, 'Stage profile, HDTF self-driven',
  `n=${hdtfSelf.length} clips from the HDTF test split, 751 frames each, ${Math.min(...clips.map((c) => c.width))}-${Math.max(...clips.map((c) => c.width))} px square, through the deployed production build.`);

stageTable(hdtfCross, 'Stage profile, HDTF cross-driven',
  `n=${hdtfCross.length} pairs, one speaker's audio driving another's video — the zero-shot task, where no ground truth exists by construction.`);

// ---------------------------------------------------------------- run-to-run spread
P('## Run-to-run spread\n');
P('Coefficient of variation on total pipeline time, per cohort. The A/B arms are tight');
P('enough to resolve a sub-1% effect; the production cohorts are not, which is why the');
P('arms were run on a dedicated machine.\n');
P('| Cohort | n | mean (s) | stdev (s) | CV |');
P('|---|---:|---:|---:|---:|');
for (const [label, runs] of [['Production lipsync', lipsync], ['Production word replacement', wr],
  ['HDTF self-driven', hdtfSelf], ['HDTF cross-driven', hdtfCross]]) {
  const v = runs.map(pipelineOf);
  P(`| ${label} | ${runs.length} | ${fmt(mean(v))} | ${fmt(stdev(v))} | ${fmt(cv(v), 3)} |`);
}
const armWalls = arms.filter((a) => a.arm === 'arm0' && a.wall_s).map((a) => a.wall_s);
if (armWalls.length) P(`| A/B baseline arm, dedicated A100 | ${armWalls.length} | ${fmt(mean(armWalls))} | ${fmt(stdev(armWalls))} | ${fmt(cv(armWalls), 3)} |`);
P('');

// ---------------------------------------------------------------- the A/B arms
/** The order the arms were run in. arm0b repeats arm0 last, as a drift control. */
const ARM_ORDER = ['arm0', 'arm1', 'arm2', 'arm3', 'arm0b', 'control_main'];

const ARM_LABEL = {
  arm0: 'Baseline: batch 4, fp32', arm1: 'Renderer batch 4 to 16', arm2: 'Batch 16 + fp16 autocast',
  arm3: 'cuDNN autotuning in the render loop', arm0b: 'Baseline, repeated (seeded)',
  control_main: 'Control: main branch runtime code',
};
const byArm = {};
for (const a of arms) (byArm[a.arm] ??= []).push(a);
const wallOf = (rows) => Object.fromEntries(rows.filter((r) => r.wall_s).map((r) => [r.clip, r.wall_s]));

P('## Optimization arms, paired per clip\n');
P('One dedicated A100 40GB, the same three clips in the same order, one warm process per');
P('arm, sampler seeded, run sequentially. Negative means the change is faster. The gate');
P('requires at least 3% faster before a change is kept.\n');
P('| Arm | Configuration | n | mean wall (s) | paired vs baseline | per-clip range | signs | verdict |');
P('|---|---|---:|---:|---:|---|---|---|');
const base = wallOf(byArm.arm0 ?? []);
for (const id of ARM_ORDER) {
  const rows = byArm[id];
  if (!rows) continue;
  const w = wallOf(rows);
  const vals = Object.values(w);
  // An arm still in flight has logs but no summary yet. Omit it rather than print NaN.
  if (!vals.length) continue;
  if (id === 'arm0') {
    P(`| arm0 | ${ARM_LABEL[id]} | ${vals.length} | ${fmt(mean(vals))} | reference | reference | | baseline |`);
    continue;
  }
  const d = pairedDelta(w, base);
  const verdict = !d ? 'incomplete'
    : id === 'arm0b' ? 'drift control'
    : id === 'control_main' ? 'control'
    : d.meanPct <= -3 ? 'KEEP' : 'rejected';
  P(`| ${id} | ${ARM_LABEL[id]} | ${vals.length} | ${fmt(mean(vals))} | ${d ? `${signed(d.meanPct)}%` : '—'} | ${d ? `${signed(d.minPct)}% to ${signed(d.maxPct)}%` : '—'} | ${d ? (d.signsConsistent ? 'consistent' : 'mixed') : '—'} | ${verdict} |`);
}
P('');

P('### Per-clip arm detail\n');
P('The paired comparison in full, so the spread can be checked against the effect.\n');
P('| Arm | Clip | baseline (s) | arm (s) | delta (s) | delta |');
P('|---|---|---:|---:|---:|---:|');
for (const id of ['arm1', 'arm2', 'arm3', 'arm0b', 'control_main']) {
  const rows = byArm[id];
  if (!rows) continue;
  const d = pairedDelta(wallOf(rows), base);
  if (!d) continue;
  for (const c of d.perClip) {
    P(`| ${id} | ${c.clip.replace(/^hdtf\d+_/, '')} | ${fmt(c.base)} | ${fmt(c.cand)} | ${signed(c.cand - c.base)} | ${signed(c.deltaPct)}% |`);
  }
}
P('');

// ---------------------------------------------------------------- session drift
const a0 = wallOf(byArm.arm0 ?? []);
const a0b = wallOf(byArm.arm0b ?? []);
if (Object.keys(a0b).length) {
  const drift = pairedDelta(a0b, a0);
  P('## Session drift, and what it does to the arms\n');
  P('arm0b repeats the baseline configuration exactly, at the end of the session, on the');
  P('same clips. Any difference is the machine, not the code. It is the control that decides');
  P('whether the arm effects above are real.\n');
  P('| | mean wall (s) | paired vs first baseline | per-clip |');
  P('|---|---:|---:|---|');
  P(`| arm0, run first | ${fmt(mean(Object.values(a0)))} | reference | reference |`);
  P(`| **arm0b, run last, identical config** | **${fmt(mean(Object.values(a0b)))}** | **${signed(drift.meanPct)}%** | ${drift.perClip.map((c) => signed(c.deltaPct)).join(', ')}% |`);
  P('');
  P(`The baseline itself moved **${signed(drift.meanPct)}%** across the session with no code change.`);
  P('That is larger than two of the three arm effects and indistinguishable from the third,');
  P('so the arms cannot be read against the first baseline alone.\n');
  P('### Arms, adjusted for drift\n');
  P('Treating the drift as linear in run order, each arm is compared against the baseline');
  P('interpolated to its own slot rather than against the first baseline. This is a');
  P('correction of last resort — the right fix is to interleave the baseline between arms —');
  P('but it bounds how much of each effect was the machine.\n');
  P('| Arm | slot | vs first baseline | vs drift-adjusted baseline | still rejected |');
  P('|---|---:|---:|---:|---|');
  const cand = ARM_ORDER.filter((id) => byArm[id] && id !== 'arm0' && id !== 'arm0b' && id !== 'control_main');
  cand.forEach((id, i) => {
    const w = wallOf(byArm[id]);
    const frac = (i + 1) / (cand.length + 1);
    const adj = Object.fromEntries(Object.keys(a0).map((k) => [k, a0[k] + frac * ((a0b[k] ?? a0[k]) - a0[k])]));
    const raw = pairedDelta(w, a0);
    const cor = pairedDelta(w, adj);
    P(`| ${id} | ${i + 2} of ${cand.length + 2} | ${signed(raw.meanPct)}% | ${signed(cor.meanPct)}% | ${cor.meanPct > -3 ? 'yes' : 'NO — revisit'} |`);
  });
  P('');
  P('Every arm stays far from the 3% gate either way, so no verdict changes. What changes is');
  P('the *reason*: adjusted for drift the three effects fall within roughly half a percent of');
  P('zero, which is a tighter null than the raw numbers suggested, and the largest raw effect');
  P('is mostly the machine rather than the change.\n');
}

// ---------------------------------------------------------------- the control arm
const ctl = wallOf(byArm.control_main ?? []);
if (Object.keys(ctl).length && Object.keys(a0b).length) {
  P('## The control: our code against the unmodified branch\n');
  P('The control runs the unmodified branch’s runtime code with only the build fixes it');
  P('needs to run at all, on the same clips. It is the comparison that separates the effect');
  P('of the code changes from the effect of rebuilding the environment.\n');
  P('It ran immediately after the repeated baseline, so those two are adjacent in time and');
  P('the drift between them is negligible. That pairing is the one to read; comparing either');
  P('against the first baseline mixes in the session drift.\n');
  const vsRepeat = pairedDelta(ctl, a0b);
  const vsFirst = pairedDelta(ctl, a0);
  P('| Comparison | slots | paired difference | per-clip | signs |');
  P('|---|---|---:|---|---|');
  P('| **Unmodified branch vs our code** | 6 vs 5, adjacent | **' + signed(vsRepeat.meanPct) + '%** | '
    + vsRepeat.perClip.map((c) => signed(c.deltaPct)).join(', ') + '% | ' + (vsRepeat.signsConsistent ? 'consistent' : 'mixed') + ' |');
  P('| Unmodified branch vs first baseline | 6 vs 1, drift mixed in | ' + signed(vsFirst.meanPct) + '% | '
    + vsFirst.perClip.map((c) => signed(c.deltaPct)).join(', ') + '% | ' + (vsFirst.signsConsistent ? 'consistent' : 'mixed') + ' |');
  P('');
  P('**The branch is performance-neutral.** Against the adjacent baseline the difference is');
  P(signed(vsRepeat.meanPct) + '% with mixed per-clip signs, which is the signature of noise');
  P('rather than an effect. So the always-on changes carried by the branch — asynchronous');
  P('host-to-device copies and the restructured render loop — do not move wall-clock either');
  P('way, and the arms measured the switches they were testing rather than incidental');
  P('differences between the branch and the trunk.\n');
}

// ---------------------------------------------------------------- output difference
let diff = null;
try { diff = raw('output-diff'); } catch { /* not measured on this checkout */ }
if (diff) {
  const byArmD = {};
  for (const r of diff.rows) (byArmD[r.arm] ??= []).push(r);
  const stat = (a) => {
    const rs = byArmD[a] ?? [];
    if (!rs.length) return null;
    return {
      n: rs.length,
      psnr: mean(rs.map((r) => r.psnr)),
      psnrMin: Math.min(...rs.map((r) => r.psnr)),
      ssim: mean(rs.map((r) => r.ssim)),
      worst: Math.max(...rs.map((r) => r.worst)),
      identical: rs.reduce((a2, r) => a2 + r.identical, 0),
    };
  };
  const floorD = stat('arm0b');
  P('## Output difference against the baseline\n');
  P('Per-frame agreement with the baseline arm. The first row is the same configuration and');
  P('the same seed, simply run again, so it **is** the noise floor on this hardware; every');
  P('other row has to be read against it rather than against zero.\n');
  P('| Compared with baseline | clips | PSNR mean (dB) | PSNR min | SSIM mean | worst pixel /255 | bit-identical frames |');
  P('|---|---:|---:|---:|---:|---:|---:|');
  for (const id of ['arm0b', 'arm1', 'arm2', 'arm3']) {
    const t = stat(id);
    if (!t) continue;
    const label = id === 'arm0b' ? '**' + ARM_LABEL[id] + ' — the floor**' : ARM_LABEL[id];
    P('| ' + label + ' | ' + t.n + ' | ' + fmt(t.psnr) + ' | ' + fmt(t.psnrMin) + ' | '
      + fmt(t.ssim, 5) + ' | ' + t.worst + ' | ' + t.identical + ' |');
  }
  P('');
  if (floorD) {
    P('### Every arm sits inside the floor\n');
    P('| Arm | PSNR vs baseline | difference from the floor | verdict |');
    P('|---|---:|---:|---|');
    for (const id of ['arm1', 'arm2', 'arm3']) {
      const t = stat(id);
      if (!t) continue;
      const d = t.psnr - floorD.psnr;
      P('| ' + ARM_LABEL[id] + ' | ' + fmt(t.psnr) + ' dB | ' + signed(d) + ' dB | '
        + (Math.abs(d) < 0.5 ? 'inside the floor' : 'OUTSIDE the floor') + ' |');
    }
    P('');
    P('**No arm changed the output beyond what re-running the identical configuration does.**');
    P('The floor is ' + fmt(floorD.psnr) + ' dB with a worst pixel of ' + floorD.worst + ' of 255,');
    P('and the largest arm deviation from it is under a quarter of a decibel. Any statement');
    P('that a precision change cost fidelity here is not supported by this measurement.\n');
    P('Note also that **' + floorD.identical + ' frames of ' + (floorD.n * 751) + ' were bit-identical**');
    P('between two runs of the same configuration with the same seed. Seeding is necessary for');
    P('a comparable A/B and is demonstrably not sufficient for reproducibility: the residual');
    P('comes from CUDA-level nondeterminism outside the seeded generators.\n');
  }
}

// ---------------------------------------------------------------- parse argmax
{
  const pa = raw('parse-argmax');
  const pct = (a, b) => 100 * (a - b) / b;
  const clipD = pa.clips.map((c) => pct(c.on, c.off));
  const meanOff = mean(pa.clips.map((c) => c.off));
  const meanOn = mean(pa.clips.map((c) => c.on));
  const meanD = mean(clipD);
  const tr = pa.transfer;
  const perFrameRatio = tr.logitBytesPerFrame / tr.classBytesPerFrame;

  P('## A change that works and is rejected anyway\n');
  P('The parsing stage reduced a 19-class, 512-square floating-point tensor **on the host,');
  P('one frame at a time** — copying about 20 MB per frame across the bus and blocking on');
  P('each copy. Reducing on the device first and shipping a single-byte class map instead is');
  P('the same arithmetic in the other order.\n');

  P('| Clip | host reduce (s) | device reduce (s) | delta |');
  P('|---|---:|---:|---:|');
  pa.clips.forEach((c, i) => {
    P(`| ${c.clip} | ${fmt(c.off)} | ${fmt(c.on)} | **${signed(clipD[i])} %** |`);
  });
  P(`| **mean** | **${fmt(meanOff)}** | **${fmt(meanOn)}** | **${signed(meanD)} %** |`);
  P('');
  P(`The stage it targets falls ${fmt(pa.stage.off)} s to ${fmt(pa.stage.on)} s,`);
  P(`**${signed(pct(pa.stage.on, pa.stage.off))} %**, and that ${fmt(pa.stage.off - pa.stage.on)} s`);
  P(`covers the job\u2019s ${fmt(meanOff - meanOn)} s. Every clip is faster, the effect is`);
  P(`${fmt(Math.abs(meanD) / 0.95, 1)}x the repeat spread, and the treated arm ran second so drift`);
  P('works against it.\n');

  P('| what moved across the bus | before | after |');
  P('|---|---:|---:|');
  P(`| per frame | ${fmt(tr.logitBytesPerFrame / 1e6, 1)} MB | ${fmt(tr.classBytesPerFrame / 1e3, 0)} KB |`);
  P(`| per ${tr.frames}-frame job | ${fmt(tr.logitBytesPerFrame * tr.frames / 1e9, 1)} GB `
    + `| ${fmt(tr.classBytesPerFrame * tr.frames / 1e9, 3)} GB |`);
  P(`| blocking transfers | ${tr.syncsBefore} | ${tr.syncsAfter} |`);
  P('');
  P(`That is ${fmt(perFrameRatio, 0)}x less traffic per frame. Output is unaffected:`);
  P(`${fmt(pa.outputDiff.psnr)} dB against the untreated arm, inside the same-configuration`);
  P(`population, and ${pa.outputDiff.sanityPassed} of ${pa.outputDiff.sanityTotal} outputs pass the`);
  P('sanity checks.\n');

  P('### And it is rejected, because the gate says so\n');
  P(`The rule applied to every candidate in this work is **at least ${fmt(pa.gate.thresholdPct, 0)} %`);
  P(`paired improvement at the job level**. This is ${fmt(Math.abs(meanD))} %. By the rule as`);
  P('written it is **rejected**, and it is left rejected.\n');
  P('The reason for saying that so plainly is that the gate was fixed before any of these');
  P('measurements were taken, and three roadmap items were rejected against it. Reaching for');
  P('a stage-level threshold at this point — because this happens to be a result worth');
  P('having, and it clears a third of the stage it touches — is exactly how a rule set in');
  P('advance stops meaning anything.\n');
  P('**What the result does expose is a property of the rule.** A job-level threshold');
  P('rejects any change confined to a stage worth less than that threshold, however complete');
  P('the win inside it. This one removed a third of its stage and still failed. Whether the');
  P('gate should be job-level at all is a decision worth making deliberately, and it is not');
  P('one a measurement can make.\n');
}

// ---------------------------------------------------------------- focal search
{
  const fs = raw('focal-search');
  const t = fs.timing;
  const pct = (a, b) => 100 * (a - b) / b;
  const clipD = t.clips.map((c) => pct(c.batched, c.sequential));
  const meanSeq = mean(t.clips.map((c) => c.sequential));
  const meanBat = mean(t.clips.map((c) => c.batched));
  const focals = fs.reads.map((r) => r.focal);
  const errs = fs.reads.map((r) => r.projError);
  const errSpread = 100 * (Math.max(...errs) - Math.min(...errs)) / Math.min(...errs);
  const seqF = fs.reads.filter((r) => r.path.startsWith('sequential')).map((r) => r.focal);
  const batF = fs.reads.filter((r) => r.path.startsWith('batched')).map((r) => r.focal);

  P('## The camera calibration does not agree with itself\n');
  P('This began as a check on an optimization and ended somewhere more important.\n');
  P('The pipeline calibrates a camera focal by sweeping 46 candidates, each a fresh');
  P('300-iteration solve, and exporting one integer. That integer is not a diagnostic: it');
  P('configures the mesh renderer and feeds every landmark projection, so it determines the');
  P('whole 3D tracking geometry for the job. Batching those 46 independent solves into one');
  P('is worth real time, and checking that it picked the same focal is what exposed this.\n');

  P('### Four reads, one clip, one seed\n');
  P('| Run | Path | Selected focal | Projection error |');
  P('|---:|---|---:|---:|');
  for (const r of fs.reads) {
    const em = r.path.startsWith('sequential');
    P(`| ${r.run} | ${em ? '**' + r.path + '**' : r.path} | ${em ? '**' + r.focal + '**' : r.focal} `
      + `| ${r.projError.toFixed(9)} |`);
  }
  P('');
  P(`**The two runs of the unmodified path chose ${seqF[0]} and ${seqF[1]}** — same code, same`);
  P(`seed, same clip, a difference of ${Math.abs(seqF[0] - seqF[1])}, or`);
  P(`${fmt(100 * Math.abs(seqF[0] - seqF[1]) / Math.min(...seqF))} % of the smaller value.\n`);
  P(`And every projection error sits within **${fmt(errSpread)} %** of every other. The`);
  P('objective is flat across most of the search range, so the choice is settled by');
  P('numerical noise rather than by the data — and the pipeline reproduces zero of 751');
  P('frames at a fixed seed, so there is always noise available to settle it. Note which run');
  P(`had the *lowest* error: run 1, the ${Math.max(...focals)} outlier. It genuinely fit best that`);
  P('time. That is what a global minimum wandering across a plateau looks like.\n');

  P('| Path | draws | spread |');
  P('|---|---|---:|');
  P(`| sequential | ${seqF.join(', ')} | **${Math.abs(seqF[0] - seqF[1])}** |`);
  P(`| batched | ${batF.join(', ')} | **${Math.abs(batF[0] - batF[1])}** |`);
  P('');
  P('Two draws each is far too few to claim the batched path is *more* stable, and that is');
  P('not claimed here. What the four reads establish is that **neither path is');
  P('reproducible**, and that the unmodified one has the wider spread of the two.\n');

  P('### What it cost to find, and what the batching is worth\n');
  P('| Clip | sequential (s) | batched (s) | delta |');
  P('|---|---:|---:|---:|');
  t.clips.forEach((c, i) => {
    P(`| ${c.clip} | ${fmt(c.sequential)} | ${fmt(c.batched)} | **${signed(clipD[i])} %** |`);
  });
  P(`| **mean** | **${fmt(meanSeq)}** | **${fmt(meanBat)}** | **${signed(mean(clipD))} %** |`);
  P('');
  P(`The tracking stage falls ${fmt(t.trackFaceOff)} s to ${fmt(t.trackFaceOn)} s,`);
  P(`${signed(pct(t.trackFaceOn, t.trackFaceOff))} %, as ${t.iterationsSequential.toLocaleString()}`);
  P(`sequential iterations become ${t.iterationsBatched.toLocaleString()}. The speedup is real.`);
  P('**Its equivalence is withdrawn**: the per-candidate losses are bit-identical, the');
  P('selection is not preserved, and it now appears there was never a stable selection to');
  P('preserve.\n');
  if (t.variant === 'unconfirmed') {
    P('### Which variant this number belongs to\n');
    P(`**${fmt(mean(clipD))} % is a ceiling, not the shipped configuration.** It measures the`);
    P('sweep batched outright. The implementation that was kept instead hands the top-ranked');
    P('candidates back to the untouched sequential solver to confirm the winner — precisely');
    P('because no batched formulation of this optimiser is bit-identical to the sequential');
    P('one, which is the wall described above.\n');
    P(`That confirmation step trades a ${t.iterationReductionUnconfirmed}x reduction in solver`);
    P(`iterations for ${t.iterationReductionConfirmed}x, so the default should land materially`);
    P('below the figure in the table. It is the better trade — it protects the one value the');
    P('rest of the tracking stage depends on, at a third of the win. **The shipped');
    P('configuration has not been timed separately, and this page does not estimate it.**\n');
  }

  P('### Two things worth taking from this\n');
  P('**The value was invisible.** Not one line of that module\u2019s logging reaches any run');
  P('log, so no production job could report the focal it chose, and nobody could have');
  P('noticed this. One print statement exposed it. That is the second time in this work that');
  P('adding a single log line turned up a real defect — the first was a silent frame-dropping');
  P('bug on the shipping path.\n');
  P('**It looked like a cause for something written off as irreducible, and it is not.**');
  P('This work measured that requesting deterministic kernels buys no reproducibility and');
  P('concluded the residual lives in libraries outside the framework\u2019s control. An');
  P('ill-conditioned selection amplifying a tiny numerical difference into a large change in');
  P('camera geometry looked like a better candidate, and it was testable, so it was tested.\n');

  const pn = fs.pinned;
  const up = pn.unpinnedPopulation;
  P('### Tested by pinning the focal, and refuted\n');
  P('| comparison | PSNR | worst pixel | mean abs | frames identical |');
  P('|---|---:|---:|---:|---:|');
  P(`| **focal pinned to ${pn.focal}, two runs** | **${fmt(pn.diff.psnr)} dB** | **${pn.diff.worst}** `
    + `| **${fmt(pn.diff.meanAbs, 1)}** | ${pn.diff.identical} of ${pn.diff.frames} |`);
  P(`| *unpinned, same-configuration population* | ${fmt(up.psnrMin)} \u2013 ${fmt(up.psnrMax)} `
    + `| ${up.worstMin} \u2013 ${up.worstMax} | ${fmt(up.meanAbsMin, 1)} \u2013 ${fmt(up.meanAbsMax, 1)} `
    + `| 0 of ${pn.diff.frames} |`);
  P('');
  P('**The pinned pair sits inside the unpinned range on every statistic.** Removing the');
  P('unstable focal removes none of the run-to-run variation, so the earlier explanation');
  P('stands and this one is withdrawn.\n');

  P('### The synthesis, which is worth more than either result alone\n');
  P('Three measurements now fit together: the objective is **flat**, so the choice is');
  P('**unstable**, and pinning it **changes nothing**. Those are one fact seen from three');
  P('sides. A flat objective means the candidate focals are *genuinely equivalent fits* —');
  P('pose and depth absorb the difference, the projections land in the same place, and the');
  P('output cannot tell which focal was used. The calibration is **under-determined, not');
  P('wrong**.\n');
  P('So the large swing in the exported value is a real reproducibility defect **and** not a');
  P('quality defect in the output. Both halves are measured, and either one alone would have');
  P('been misleading.\n');
  P('### What it does license\n');
  P(`Pinning skipped the search entirely and ran ${fmt(100 * (mean(pn.runs.map((r) => r.wall_s)) - pn.sweepWall) / pn.sweepWall)} %`);
  P(`faster than the sweep — slightly better than batching it, because it does no search at`);
  P('all. If the output is insensitive to which focal is chosen, 46 solves are buying very');
  P('little. A hardcoded focal is not the fix, since it would be wrong for a clip whose true');
  P('focal differs; a coarser sweep, or a cheap initialisation plus one refinement, should');
  P('land inside the same plateau at a fraction of the cost. Unlike the three rejected');
  P('arms, the mechanism here predicts it should work.\n');
}

// ---------------------------------------------------------------- frame cache
{
  const fc = raw('frame-cache');
  const pct = (on, off) => 100 * (on - off) / off;
  const clipD = fc.clips.map((c) => pct(c.on, c.off));
  const meanOff = mean(fc.clips.map((c) => c.off));
  const meanOn = mean(fc.clips.map((c) => c.on));
  const meanD = mean(clipD);

  P('## The change that was not null: caching decoded frames\n');
  P('Every optimization above returned null. This one did not, and it is worth more than');
  P('all of them together by a wide margin.\n');
  P('The renderer reads frames through a reader with exactly one fast path: the next frame');
  P('in sequence. Its dataset asks for five frames around each position, so after finishing');
  P('one item at position +2 the next item asks for -1 — a backward step of a single frame.');
  P('That misses the fast path, and because the intermediate videos are written with a');
  P('250-frame keyframe interval, stepping back one frame re-decodes from the previous');
  P('keyframe. Four of the five frames each item needs were decoded moments earlier, so');
  P('keeping the last five turns that backward seek into a hit.\n');

  P('### Wall clock, paired per clip\n');
  const meanRep = mean(fc.clips.map((c) => c.onRepeat));
  const repD = mean(fc.clips.map((c) => pct(c.onRepeat, c.off)));
  P('| Clip | cache off (s) | cache on (s) | cache on, repeated (s) | delta | repeat delta |');
  P('|---|---:|---:|---:|---:|---:|');
  fc.clips.forEach((c, i) => {
    P(`| ${c.clip} | ${fmt(c.off)} | ${fmt(c.on)} | ${fmt(c.onRepeat)} `
      + `| **${signed(clipD[i])} %** | ${signed(pct(c.onRepeat, c.off))} % |`);
  });
  P(`| **mean** | **${fmt(meanOff)}** | **${fmt(meanOn)}** | **${fmt(meanRep)}** `
    + `| **${signed(meanD)} %** | **${signed(repD)} %** |`);
  P('');
  P(`The cache-on configuration was run twice, independently, landing`);
  P(`${fmt(Math.abs(100 * (meanRep - meanOn) / meanOn))} % apart. The effect is not a one-off.\n`);
  P(`All three clips in the same direction. Against the 3 % gate this is`);
  P(`${fmt(Math.abs(meanD) / 3, 1)}x, and against the repeat spread measured on four identical`);
  P(`runs it is ${fmt(Math.abs(meanD) / 0.95, 0)}x.\n`);
  P('**Both confounds favour the slower arm**, so this is a floor and not a ceiling: the');
  P('cache-off arm ran first, and drift makes later runs slower; and it ran into a fresher');
  P('output directory, which the drift result associates with being faster.\n');

  P('### The saving is where the mechanism predicts, and nowhere else\n');
  P('| Stage | cache off (s) | cache on (s) | delta (s) | delta |');
  P('|---|---:|---:|---:|---:|');
  for (const st of fc.stages) {
    const d = pct(st.on, st.off);
    const em = Math.abs(d) > 20;
    const nm = em ? `**${st.stage}**` : st.stage;
    P(`| ${nm} | ${fmt(st.off)} | ${fmt(st.on)} | ${signed(st.on - st.off)} `
      + `| ${em ? '**' + signed(d) + ' %**' : signed(d) + ' %'} |`);
  }
  P('');
  const r = fc.stages.find((x) => x.stage === 'render_rgb');
  P(`**The neural render stage falls ${fmt(Math.abs(pct(r.on, r.off)))} %, and its`);
  P(`${fmt(Math.abs(r.on - r.off))} s accounts for the job's ${fmt(Math.abs(meanOn - meanOff))} s.**`);
  P('No other stage moves by more than 3 %. The change touches one reader and the time');
  P(`disappears from the one stage that reads through it: ${fmt(1000 * r.off / fc.renderFrames, 0)} ms`);
  P(`per frame down to ${fmt(1000 * r.on / fc.renderFrames, 0)} ms.\n`);

  P('### Why this explains the three nulls\n');
  P(`The renderer was not slow because its arithmetic was slow. It was`);
  P(`${fmt(100 * (r.off - r.on) / r.off, 0)} % waiting for frames. Raising the batch size,`);
  P('halving the precision and autotuning the kernels all made the arithmetic cheaper — and');
  P('the arithmetic was never what the stage was spending its time on. The mechanism');
  P('identified after those three nulls predicted this result before it was run, including');
  P(`its size: the audit said each frame was being decoded about five times, and a local`);
  P(`fixture then counted exactly ${fc.fixture.decodesOff} decodes for ${fc.fixture.frames} frames`);
  P(`before the cache and ${fc.fixture.decodesOn} after, with the backward seeks going from`);
  P(`${fc.fixture.backwardSeeksOff} to ${fc.fixture.backwardSeeksOn}.\n`);

  P('### Does it change the output?\n');
  const od = fc.outputDiff;
  const rng = (rows, k) => {
    const v = rows.map((r) => r[k]).filter((x) => x !== null && x !== undefined);
    return [Math.min(...v), Math.max(...v)];
  };
  P('A hypothesis was tested and refuted first, because it decides how the rest reads.');
  P('The two readers might have *disagreed about what frame `i` is*: this library\u2019s');
  P('frame-index seek is widely reported to be inexact on H.264, in which case the uncached');
  P('reader would return different pixels depending on whether it reached an index by');
  P('seeking or by reading forward, and the cache would legitimately differ from it.\n');
  P(`Measured directly on a real pipeline output: **${fc.seekExactness.identical} of`);
  P(`${fc.seekExactness.indices} indices identical** whether reached by seek or by reading`);
  P('forward. Seeking is frame-exact here, so that hypothesis is false — and its refutation');
  P('is what generalises the exactness argument. A hit returns what the decoder returned;');
  P('decoding an index is deterministic; therefore the cache returns what the uncached');
  P('reader would for **any** access pattern, not only the one the fixture replays.\n');
  P('So the renderer sees identical input either way, and any remaining difference is the');
  P('pipeline\u2019s own nondeterminism — which is not small: it reproduces zero of 751');
  P('frames between two runs of one seed. The comparison below is therefore against every');
  P('same-configuration pair measured in this work, not against zero.\n');

  const [tw0, tw1] = rng(od.treatment, 'worst');
  const [iw0, iw1] = rng(od.identicalConfig, 'worst');
  const [tm0, tm1] = rng(od.treatment, 'meanAbs');
  const [im0, im1] = rng(od.identicalConfig, 'meanAbs');
  const tp = mean(od.treatment.map((r) => r.psnr));
  const ip = mean(od.identicalConfig.map((r) => r.psnr));
  P('| Statistic | same configuration, re-run | cache off vs on | overlap |');
  P('|---|---|---|---|');
  P(`| mean PSNR | ${fmt(ip)} dB | ${fmt(tp)} dB | indistinguishable, ${fmt(Math.abs(tp - ip))} dB apart |`);
  P(`| mean absolute difference | ${fmt(im0, 1)} \u2013 ${fmt(im1, 1)} | ${fmt(tm0, 1)} \u2013 ${fmt(tm1, 1)} | ${tm0 < im1 ? 'yes' : 'no'} |`);
  P(`| worst pixel | ${iw0} \u2013 ${iw1} | ${tw0} \u2013 ${tw1} | ${tw0 <= iw1 ? 'yes, at ' + iw1 : 'no'} |`);
  P('');
  P('**A correction, in the open.** With only the immediate repeat control in hand, this');
  P('work concluded that the treatment showed no overlap with the noise and therefore');
  P('changed the output. Against the full set of same-configuration comparisons that is');
  P('wrong. The error was leaning on **worst pixel**, which is a maximum over 751 frames and');
  P('every pixel of each — an extreme-value statistic, heavy-tailed, and a poor');
  P('discriminator at three clips. The robust statistics overlap and mean PSNR differs by');
  P(`${fmt(Math.abs(tp - ip))} dB.\n`);
  P('**The cache is output-neutral**, on the argument and on the robust measurements. It');
  P('stays switched off by default all the same: enabling it is a deployment decision that');
  P('wants a wider validation set than three clips, which is a different thing from wanting');
  P('more evidence of this kind.\n');
}

// ---------------------------------------------------------------- output sanity
{
  const san = raw('output-sanity');
  const rows = san.rows;
  if (rows.length) {
    const failed = rows.filter((r) => r.verdict !== 'OK');
    const minMouth = Math.min(...rows.map((r) => r.mouthMotion));
    const frames = [...new Set(rows.map((r) => r.frames))];
    P('## Were the arm outputs actually valid videos?\n');
    P('Every verdict above rests on the arms having produced real video. A timing harness');
    P('cannot see a black frame, a frozen output, or a face whose mouth never moves, so an');
    P('arm that ran faster because it emitted garbage would read as a win. This was asserted');
    P('in earlier versions of this page; here it is the measurement.\n');

    const byArm = {};
    for (const r of rows) (byArm[r.arm] ??= []).push(r);
    P('| Arm | clips checked | frames each | lowest mean luma | frozen frames | lowest mouth motion | verdict |');
    P('|---|---:|---:|---:|---:|---:|---|');
    for (const [arm, rs] of Object.entries(byArm)) {
      P(`| ${arm} | ${rs.length} | ${[...new Set(rs.map((r) => r.frames))].join(', ')} `
        + `| ${fmt(Math.min(...rs.map((r) => r.lumaMean)), 1)} `
        + `| ${rs.reduce((a, r) => a + r.identicalConsecutive, 0)} `
        + `| ${fmt(Math.min(...rs.map((r) => r.mouthMotion)), 2)} `
        + `| ${rs.every((r) => r.verdict === 'OK') ? 'all pass' : '**FAILURE**'} |`);
    }
    P('');
    P(`**${rows.length - failed.length} of ${rows.length} outputs pass, and nothing is marginal.**`);
    P(`Every output is ${frames.join('/')} frames, no run contains a single frozen frame, and the`);
    P(`weakest lower-face motion is ${fmt(minMouth, 2)} against a static-mouth threshold of`);
    P(`${san.staticMouthThreshold} — a factor of ${fmt(minMouth / san.staticMouthThreshold, 1)} clear of it.`);
    P('So the nulls are nulls: every arm did the work and produced a talking face.\n');
  }
}

// ---------------------------------------------------------------- drift cause
{
  const arms = raw('arms');
  const wall = (arm, clip) => {
    const r = arms.find((x) => x.arm === arm && x.clip === clip);
    return r ? r.wall_s : null;
  };
  const clips = [...new Set(arms.filter((r) => r.arm === 'arm0c').map((r) => r.clip))];
  if (clips.length) {
    const pct = (a, b) => 100 * (a - b) / b;
    const rows = clips.map((c) => ({
      clip: c.replace(/^hdtf\d+_/, ''),
      a0: wall('arm0', c), a0b: wall('arm0b', c), a0c: wall('arm0c', c),
    })).filter((r) => r.a0 && r.a0b && r.a0c);
    const vs0 = rows.map((r) => pct(r.a0c, r.a0));
    const vs0b = rows.map((r) => pct(r.a0c, r.a0b));
    const m0 = mean(vs0), m0b = mean(vs0b);
    const driftPct = pct(mean(rows.map((r) => r.a0b)), mean(rows.map((r) => r.a0)));

    P('## Where the session drift came from\n');
    P('The repeated baseline moved the whole session by a figure larger than every effect');
    P('under test, so its cause is worth more than any of the arms. One hypothesis was');
    P('mechanical: the pipeline writes eighteen intermediate videos per job into one');
    P('directory, and that directory had grown to 20 GB over the session. So the baseline');
    P('was run a third time with the directory cleared first, and nothing else changed.\n');
    P('Free space was **not** the mechanism — the volume stayed 39 % full throughout, so');
    P('any effect has to come from directory contents rather than from running out of room.\n');

    P('| Clip | first baseline (s) | last baseline (s) | cleared (s) | cleared vs first | cleared vs last |');
    P('|---|---:|---:|---:|---:|---:|');
    for (let i = 0; i < rows.length; i++) {
      P(`| ${rows[i].clip} | ${fmt(rows[i].a0)} | ${fmt(rows[i].a0b)} | ${fmt(rows[i].a0c)} | `
        + `${signed(vs0[i])} % | ${signed(vs0b[i])} % |`);
    }
    P(`| **mean** | **${fmt(mean(rows.map((r) => r.a0)))}** | **${fmt(mean(rows.map((r) => r.a0b)))}** `
      + `| **${fmt(mean(rows.map((r) => r.a0c)))}** | **${signed(m0)} %** | **${signed(m0b)} %** |`);
    P('');

    const recovered = driftPct - m0;
    P(`Clearing the directory recovered **${fmt(recovered)} of the ${fmt(driftPct)} points** of`);
    P(`drift, about ${Math.round(100 * recovered / driftPct)} % of it, and the direction is`);
    P(`consistent: all ${rows.length} clips ran faster than the uncleared baseline and all`);
    P(`${rows.length} still ran slower than the first one. So roughly half the drift is`);
    P('accumulated output and roughly half remains unexplained.\n');

    // Compared against the four-run CV of the repeat pairs, not a single pair's range:
    // a range from two samples is an unstable variance estimate. Computed from those
    // samples so this cannot drift from the determinism section.
    const detRuns = raw('determinism').runs.map((r) => r.wall_s);
    const repeatCv = 100 * stdev(detRuns) / mean(detRuns);
    P('### How much to trust this\n');
    P(`Not much, and the reason is stated here rather than buried. The effect is`);
    P(`${fmt(recovered)} points against a repeat spread of ${fmt(repeatCv)} % CV, measured over`);
    P(`${detRuns.length} runs of one identical configuration. **The effect is about the size of the`);
    P('noise it is measured against**, at n=1 per configuration. What survives is the');
    P('consistent sign across clips and a plausible mechanism, which together are worth a');
    P('cheap operational change and not a claim:\n');
    P('* clear the output directory between arms. It costs nothing and removes a confound.');
    P('* interleave a baseline between every arm, which is what should have happened here.');
    P('* do not quote the split between explained and unexplained drift as a result.\n');
  }
}

// ---------------------------------------------------------------- determinism
{
  const det = raw('determinism');
  const arms = raw('arms');
  const wall = (arm) => {
    const r = arms.find((x) => x.arm === arm && String(x.clip).includes(det.clip.replace(/^1_/, '')));
    return r ? r.wall_s : null;
  };
  const walls = det.runs.map((r) => r.wall_s);
  const detMean = mean(walls);
  const detCv = 100 * stdev(walls) / detMean;
  const range = (v) => Math.max(...v) - Math.min(...v);
  const byPair = {};
  for (const r of det.runs) (byPair[r.pair] ??= []).push(r.wall_s);

  P('## Deterministic kernels: what they cost, and what they buy\n');
  P('One clip run four times at one seed with deterministic kernels requested, as two');
  P('independent pairs. The question was whether the pipeline can be made to reproduce its');
  P('own output bit for bit, because a cache that reuses computed segments would otherwise');
  P('have no way to verify itself.\n');

  P('| pair | machine state | frames | bit-identical | PSNR mean | worst pixel |');
  P('|---|---|---:|---:|---:|---:|');
  for (const d of det.diffs) {
    P(`| ${d.pair} | ${d.state} output directory | ${d.frames} | **${d.identical}** | `
      + `${fmt(d.psnr)} dB | ${d.worst} of 255 |`);
  }
  P('');
  P('**Zero frames reproduced, in both pairs.** Asking torch for deterministic kernels leaves');
  P('run-to-run variation where seeding alone left it.\n');

  const dd = raw('output-diff');
  const fl = dd.rows.filter((r) => r.arm === 'arm0b');
  if (fl.length) {
    const flPsnr = mean(fl.map((r) => r.psnr));
    P(`For comparison the seeded-only floor is ${fmt(flPsnr)} dB, so the deterministic pairs at`);
    P(`${fmt(mean(det.diffs.map((d) => d.psnr)))} dB are no closer to reproducing themselves than`);
    P('an ordinary re-run is.\n');
  }

  const rows = [['arm0, first pass', wall('arm0')], ['arm0b, last pass', wall('arm0b')],
    ['arm0c, cleared directory', wall('arm0c')]].filter((r) => r[1]);
  if (rows.length) {
    P('### What it costs\n');
    P('| baseline for this clip | wall (s) | determinism vs it |');
    P('|---|---:|---:|');
    for (const [label, w] of rows) {
      P(`| ${label} | ${fmt(w)} | **${signed(100 * (detMean - w) / w)} %** |`);
    }
    P('');
    P(`Determinism mean is ${fmt(detMean)} s over ${walls.length} runs: a double-digit tax for`);
    P('no reproducibility gain, so it stays off.\n');
  }

  P('### Why, and what it decides about the cache\n');
  P('The nondeterminism is not in torch. Face detection and landmarks run through');
  P('onnxruntime\u2019s CUDA execution provider and rasterisation goes through nvdiffrast,');
  P('neither of which the torch setting reaches, and the setting was applied in warn-only');
  P('mode so any torch kernel lacking a deterministic implementation still proceeds.\n');
  P('**This decides the cache design.** A cache cannot validate itself by recomputing a');
  P('segment and checking the result matches, because recomputation does not reproduce');
  P('bytes. It has to store and return what it computed, and rest its correctness on how');
  P('keys are derived rather than on agreement after the fact.\n');

  P('### The repeat spread, and a correction to how it was first quoted\n');
  P('All four runs are the same code, same clip, same seed, same flags.\n');
  // CV is the column the rest of the site quotes. Range is kept beside it precisely so a
  // reader can see the difference between the two, which is what the correction is about.
  P('| | n | mean (s) | range (s) | range % | CV % |');
  P('|---|---:|---:|---:|---:|---:|');
  for (const [state, v] of Object.entries(byPair)) {
    P(`| pair with ${state} output directory | ${v.length} | ${fmt(mean(v))} | ${fmt(range(v))} `
      + `| ${fmt(100 * range(v) / mean(v))} % | ${v.length > 1 ? fmt(100 * stdev(v) / mean(v)) + ' %' : '—'} |`);
  }
  P(`| **all four** | **${walls.length}** | **${fmt(detMean)}** | **${fmt(range(walls))}** `
    + `| **${fmt(100 * range(walls) / detMean)} %** | **${fmt(detCv)} %** |`);
  P('');
  P(`**The repeat spread is best quoted as the four-run CV, ${fmt(detCv)} %.** The first pair was`);
  P(`published here as a ${fmt(100 * range(byPair.uncleared ?? walls) / mean(byPair.uncleared ?? walls))} % repeat spread, which`);
  P('was a range taken from two samples — an unstable estimate of variance, and this page');
  P('said so more confidently than two points can support. The second pair came in ten times');
  P(`tighter at ${fmt(100 * range(byPair.cleared ?? walls) / mean(byPair.cleared ?? walls))} %, while the two pairs\u2019 *means* differ by only`);
  P(`${fmt(Math.abs(100 * (mean(byPair.cleared ?? walls) - mean(byPair.uncleared ?? walls)) / mean(byPair.uncleared ?? walls)))} %.\n`);
  P('Two pairs cannot establish that clearing the output directory reduces variance, and the');
  P('tighter pair is confounded with running later. What the four runs do support is the');
  P(`headline: at a ${fmt(detCv)} % CV this rig cannot resolve the candidate effects, which came`);
  P('in at 0.14, 0.41 and 0.50 %. That conclusion is unchanged, and it is the only one the');
  P('sample size carries.\n');
}

// ---------------------------------------------------------------- resolution model
P('## Cost against input resolution\n');
P(`The HDTF clips hold frame count fixed at 751 while pixel count varies ${fmt(Math.max(...clips.map((c) => c.width ** 2)) / Math.min(...clips.map((c) => c.width ** 2)), 1)}x, `);
P('which separates stages whose cost tracks input size from those that run at a fixed');
P('internal resolution. Fitted per frame against megapixels.\n');
const byClip = Object.fromEntries(clips.map((c) => [c.id, c]));
const pts = hdtfSelf.filter((r) => byClip[r.clip]).map((r) => ({
  mp: (byClip[r.clip].width * byClip[r.clip].height) / 1e6,
  frames: r.frames ?? byClip[r.clip].frames, stages: r.stages,
}));
P('| Stage | R-squared vs pixel count | s/frame/MPix | fixed s/frame | verdict |');
P('|---|---:|---:|---:|---|');
const fits = {};
for (const s of STAGE_ORDER) {
  const xs = pts.map((p) => p.mp);
  const ys = pts.map((p) => (p.stages[s] ?? 0) / p.frames);
  const f = linfit(xs, ys);
  fits[s] = f;
  const span = Math.abs(f.b * (Math.max(...xs) - Math.min(...xs)));
  const dep = f.r2 > 0.85 && span > 0.3 * mean(ys);
  P(`| ${STAGE_LABEL[s]} | ${fmt(f.r2, 3)} | ${fmt(f.b, 5)} | ${fmt(f.a, 5)} | ${dep ? 'scales with resolution' : 'flat'} |`);
}
P('');
P('### Held-out prediction of the 1080p profile\n');
P('The model is fitted on the HDTF clips only. The production 1920x1080 profile is held');
P('out of every fit, then predicted, extrapolating beyond the largest fitted clip.\n');
const REF_FRAMES = mean(lipsync.map((r) => r.frames));
const REF_MP = (1920 * 1080) / 1e6;
P('| Stage | predicted (s) | measured (s) | error |');
P('|---|---:|---:|---:|');
const errs = [];
for (const s of STAGE_ORDER) {
  const pred = (fits[s].a + fits[s].b * REF_MP) * REF_FRAMES;
  const meas = ls.m[s];
  if (!meas) continue;
  const e = (100 * (pred - meas)) / meas;
  errs.push(Math.abs(e));
  P(`| ${STAGE_LABEL[s]} | ${fmt(pred)} | ${fmt(meas)} | ${signed(e, 1)}% |`);
}
P(`\nMedian absolute error ${fmt(percentile(errs, 0.5), 1)}%, extrapolating ${fmt(REF_MP / Math.max(...pts.map((p) => p.mp)), 1)}x beyond the largest fitted clip.\n`);

// ---------------------------------------------------------------- quality
P('## Lip-sync error against ground truth\n');
P('SyncNet under the published evaluation protocol, computed on CPU. Each generated clip');
P('is paired with its own source footage, so the comparison is within-clip. LSE-D is lower-');
P('is-better, LSE-C higher-is-better.\n');
const gen = { ...(lse.hdtf_prod?.scores ?? {}), ...(lse.hdtf_prod_rest?.scores ?? {}) };
const gt = { ...(lse.hdtf_gt?.scores ?? {}), ...(lse.hdtf_gt_rest?.scores ?? {}) };
const FLOOR_D = 0.256;
const paired = Object.keys(gen).filter((k) => k in gt && !gen[k].error && !gt[k].error)
  .map((k) => ({ clip: k.replace(/\.mp4$/, ''), gd: gen[k].lse_d, td: gt[k].lse_d, gc: gen[k].lse_c, tc: gt[k].lse_c }))
  .sort((a, b) => (a.gd - a.td) - (b.gd - b.td));
P('| | LSE-D | LSE-C | n |');
P('|---|---:|---:|---:|');
P(`| Generated | ${fmt(mean(paired.map((p) => p.gd)), 3)} | ${fmt(mean(paired.map((p) => p.gc)), 3)} | ${paired.length} |`);
P(`| Its own real footage | ${fmt(mean(paired.map((p) => p.td)), 3)} | ${fmt(mean(paired.map((p) => p.tc)), 3)} | ${paired.length} |`);
P(`| **Paired gap** | **${signed(mean(paired.map((p) => p.gd - p.td)), 3)}** | ${signed(mean(paired.map((p) => p.gc - p.tc)), 3)} | |`);
const crossScores = Object.values(lse.hdtf_cross?.scores ?? {}).filter((v) => !v.error);
if (crossScores.length) P(`| Cross-driven generated | ${fmt(mean(crossScores.map((v) => v.lse_d)), 3)} | ${fmt(mean(crossScores.map((v) => v.lse_c)), 3)} | ${crossScores.length} |`);
P('');
P('### Per-clip quality detail\n');
P(`Sorted by gap. The noise floor is ${FLOOR_D} LSE-D, measured from a single pair of`);
P('regenerations of one production clip and carried here — treat it as indicative.\n');
P('| Clip | generated LSE-D | real LSE-D | gap | within noise floor | generated LSE-C | real LSE-C |');
P('|---|---:|---:|---:|---|---:|---:|');
for (const p of paired) {
  P(`| ${p.clip.replace(/^hdtf\d+_/, '')} | ${fmt(p.gd, 3)} | ${fmt(p.td, 3)} | ${signed(p.gd - p.td, 3)} | ${Math.abs(p.gd - p.td) <= FLOOR_D ? 'yes' : 'no'} | ${fmt(p.gc, 3)} | ${fmt(p.tc, 3)} |`);
}
P(`\n${paired.filter((p) => Math.abs(p.gd - p.td) <= FLOOR_D).length} of ${paired.length} clips fall inside the floor on LSE-D; `);
P(`${paired.filter((p) => Math.abs(p.gc - p.tc) <= 0.288).length} of ${paired.length} on LSE-C.\n`);

// ---------------------------------------------------------------- where the GPU goes
P('## GPU work against CPU-only work\n');
P('Job-level stages classified by whether they require a GPU at all. The CPU-only share is');
P('billed at the GPU machine rate while using no GPU.\n');
P('| Workload | job total (s) | GPU work (s) | CPU-only (s) | CPU-only share |');
P('|---|---:|---:|---:|---:|');
for (const [label, runs] of [['Production lipsync', lipsync], ['Production word replacement', wr], ['HDTF self-driven', hdtfSelf]]) {
  let c = 0, g = 0;
  for (const r of runs) for (const [k, v] of Object.entries(r.job)) (CPU_ONLY.has(k) ? (c += v / runs.length) : (g += v / runs.length));
  P(`| ${label} | ${fmt(c + g)} | ${fmt(g)} | ${fmt(c)} | ${fmt(100 * c / (c + g), 1)}% |`);
}
P('');

// ---------------------------------------------------------------- cost
P('## Cost per video\n');
P(`Rate is an input, printed here: ${DEFAULT_RATE.label} at ${usd(DEFAULT_RATE.usdPerHour)}/GPU-hour.`);
P('The GPU model the production runs executed on is not recorded in the pipeline logs or');
P('deployment configuration, so this rate is not reconciled against that hardware.\n');
P('| Stage | seconds | $/video | $/1000 videos |');
P('|---|---:|---:|---:|');
for (const s of Object.keys(ls.m)) P(`| ${STAGE_LABEL[s]} | ${fmt(ls.m[s])} | ${usd(per1000(ls.m[s]) / 1000)} | ${usd(per1000(ls.m[s]))} |`);
P(`| **Pipeline total** | **${fmt(ls.tot)}** | ${usd(per1000(ls.tot) / 1000)} | **${usd(per1000(ls.tot))}** |`);
P('');
P('### The cacheable prefix\n');
P('The four stages before the animator take no audio input, so for any job reusing a');
P('source video they recompute identical results.\n');
const srcS = Object.entries(ls.m).filter(([k]) => SOURCE_ONLY.has(k)).reduce((a, [, v]) => a + v, 0);
const wrSrc = Object.entries(wrp.m).filter(([k]) => SOURCE_ONLY.has(k)).reduce((a, [, v]) => a + v, 0);
P('| Workload | pipeline (s) | source-only (s) | cacheable share | $/1000 saved at full hit rate |');
P('|---|---:|---:|---:|---:|');
P(`| Production lipsync | ${fmt(ls.tot)} | ${fmt(srcS)} | ${fmt(100 * srcS / ls.tot, 1)}% | ${usd(per1000(srcS))} |`);
P(`| Production word replacement | ${fmt(wrp.tot)} | ${fmt(wrSrc)} | ${fmt(100 * wrSrc / wrp.tot, 1)}% | ${usd(per1000(wrSrc))} |`);
P('');
P('### Throughput per GPU-hour\n');
P('The planning unit on hardware kept warm, where cold start is amortised rather than');
P('billed per job. Rate-independent.\n');
P('| Scenario | seconds per video | videos per GPU-hour |');
P('|---|---:|---:|');
P(`| Lipsync, as deployed | ${fmt(ls.tot)} | ${fmt(throughputPerGpuHour(ls.tot), 1)} |`);
P(`| Word replacement, as deployed | ${fmt(wrp.tot)} | ${fmt(throughputPerGpuHour(wrp.tot), 1)} |`);
P(`| Word replacement, source analysis cached | ${fmt(wrp.tot - wrSrc)} | ${fmt(throughputPerGpuHour(wrp.tot - wrSrc), 1)} |`);
P('');
P('### Rate sensitivity\n');
P('The same measured seconds, priced three ways, because the rate is the largest');
P('uncertainty in every money figure here.\n');
P('| Rate | $/GPU-hour | lipsync $/1000 | word replacement $/1000 |');
P('|---|---:|---:|---:|');
for (const r of Object.values(RATES)) P(`| ${r.label} | ${usd(r.usdPerHour)} | ${usd(per1000(ls.tot, r))} | ${usd(per1000(wrp.tot, r))} |`);
P('');

writeFileSync(join(root, 'results/measured.md'), `${out.join('\n')}\n`);
console.log(`wrote results/measured.md (${out.length} lines)`);
